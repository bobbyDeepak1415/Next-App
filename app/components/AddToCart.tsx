'use client'

import React from "react";


const AddToCart = () => {
  return (
    <div>
      <button onClick={() => console.log("added")}>Add To cart</button>
      Add To cart
    </div>
  );
};

export default AddToCart;
